# Same as repo-root amplify.yml. Prefer connecting Amplify to the repo root spec so appRoot applies.
# Monorepo: Amplify cds into appRoot before preBuild/build — no manual cd.
#
version: 1.0
applications:
  - appRoot: Apps/Web/arellsstore
    frontend:
      phases:
        preBuild:
          commands:
            - echo "🧹 Clearing build cache..."
            - rm -rf .next node_modules/.cache dist
            - node -v && npm -v
            - npx --yes yarn@1.22.22 --version
            - npx --yes yarn@1.22.22 install --frozen-lockfile
        build:
          commands:
            - echo "🚀 next build..."
            - npx --yes yarn@1.22.22 run build
      artifacts:
        baseDirectory: .next
        files:
          - '**/*'
      cache:
        paths:
          - node_modules/**/*
